import React, { PropsWithChildren } from "react";

const Wrapper1280 = ({ children }: PropsWithChildren) => {
  return <div className="max-w-7xl mx-auto">{children}</div>;
};

export default Wrapper1280;
